✦ Assistant
存储对比

粗体、斜体、删除、node:sqlite 和 文档 (https://example.com)。

┌──────────┬───────────────────────────────────────────┬───────────────────────────────────────────┐
│          │ 现在的 <sha256(sessionId)>-<cut>.json     │ node:sqlite                               │
├──────────┼───────────────────────────────────────────┼───────────────────────────────────────────┤
│ 陈旧写入 │ 靠 cut 比较 + 事后删旧文件 → 实测漏了 3   │ 一条 INSERT … ON CONFLICT(session_id) DO  │
│          │ 份旧 cut 和 3 个 v1 残留                  │ UPDATE … WHERE excluded.cut >= cut        │
│ 启动读取 │ 枚举 49 个文件逐个 JSON.parse             │ 一次 query，可按天/按会话聚合             │
│ 代数迁移 │ 文件格式版本各自为政（v1                  │ 单一 SCHEMA_VERSION                       │
│          │ 永久留存，无人清理）                      │                                           │
│ 可读性   │ 可以直接 cat / grep / 手工修              │ 需要 sqlite3 或写脚本                     │
│ 依赖     │ 0                                         │ 0（node:sqlite 在 22.13 起已不需要        │
│          │                                           │ --experimental-sqlite，但官方仍标         │
│          │                                           │ experimental；本机 Node 26 上             │
│          │                                           │ DatabaseSync/StatementSync/backup 都在）  │
└──────────┴───────────────────────────────────────────┴───────────────────────────────────────────┘

│ 引用内容

[x] 表格
[ ] 验证
    • 子项目

┌ ts
const text = "**literal** $x$";
└

┌──────┐
│      │
│ 开始 │
│      │
└───┬──┘
    │
    │
    │
    │
    ▼
┌──────┐
│      │
│ 完成 │
│      │
└──────┘

行内公式 E=mc²，分式 (1)/(x²)+α。

∑_(i=1)^(n)i=(n(n+1))/(2)

